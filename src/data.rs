use chrono::DateTime;
use std::collections::{HashMap, HashSet};

struct DataManager {}
pub fn get_ltf_candles(pair: &str, tf: &str, start: &str, end: &str) -> Result<()> {
    // is pair available
    // timeframe2seconds
    // timestamp2unixtime start and end
    // build url
    // make http get request
    // deserialize to array of Candles


}